import intro from "./intro.schema";
import resume from "./resume.schema";
import specialities from "./specialities.schema";

const schema = { types: [intro, resume, specialities] };

export default schema;
